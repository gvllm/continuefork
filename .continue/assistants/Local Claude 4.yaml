name: Local Claude 4
version: 1.0.0
schema: v1

models:
  - uses: gvllm/claude-4-sonnet  # owner/block slug avoids hub fetch

context:
  - provider: code
  - provider: diff

rules:
  - Keep explanations concise
